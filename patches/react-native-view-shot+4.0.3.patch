diff --git a/node_modules/react-native-view-shot/ios/RNViewShot.mm b/node_modules/react-native-view-shot/ios/RNViewShot.mm
index 2cf632f..354e801 100644
--- a/node_modules/react-native-view-shot/ios/RNViewShot.mm
+++ b/node_modules/react-native-view-shot/ios/RNViewShot.mm
@@ -4,7 +4,11 @@
 #import <React/UIView+React.h>
 #import <React/RCTUtils.h>
 #import <React/RCTConvert.h>
+#ifndef RCT_NEW_ARCH_ENABLED
 #import <React/RCTScrollView.h>
+#else
+#import <React/RCTScrollViewComponentView.h>
+#endif
 #import <React/RCTUIManager.h>
 #if __has_include(<React/RCTUIManagerUtils.h>)
 #import <React/RCTUIManagerUtils.h>
@@ -80,6 +84,15 @@ RCT_EXPORT_METHOD(captureRef:(nonnull NSNumber *)target
     UIView* rendered;
     UIScrollView* scrollView;
     if (snapshotContentContainer) {
+#ifdef RCT_NEW_ARCH_ENABLED
+      if (![view isKindOfClass:[RCTScrollViewComponentView class]]) {
+        reject(RCTErrorUnspecified, [NSString stringWithFormat:@"snapshotContentContainer can only be used on a ScrollView. instead got: %@", view], nil);
+        return;
+      }
+      RCTScrollViewComponentView* rctScrollView = (RCTScrollViewComponentView*)view;
+      scrollView = rctScrollView.scrollView;
+      rendered = scrollView;
+#else
       if (![view isKindOfClass:[RCTScrollView class]]) {
         reject(RCTErrorUnspecified, [NSString stringWithFormat:@"snapshotContentContainer can only be used on a RCTScrollView. instead got: %@", view], nil);
         return;
@@ -87,6 +100,7 @@ RCT_EXPORT_METHOD(captureRef:(nonnull NSNumber *)target
       RCTScrollView* rctScrollView = view;
       scrollView = rctScrollView.scrollView;
       rendered = scrollView;
+#endif
     }
     else {
       rendered = view;
